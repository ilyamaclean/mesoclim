% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workerfunctions.R
\name{get_dem}
\alias{get_dem}
\title{Download a digital elevation model matching a SpatRaster extent}
\usage{
get_dem(r, zeroasna = TRUE)
}
\arguments{
\item{r}{A \code{SpatRaster} whose extent and CRS define the download area.}

\item{zeroasna}{Logical. If \code{TRUE} (default), zero-elevation cells (sea) are
set to \code{NA} in the returned raster.}
}
\value{
A \code{SpatRaster} of elevation values (metres) on the same grid as \code{r},
or \code{NULL} if no internet connection is available.
}
\description{
Downloads elevation data from the AWS Terrain Tiles service to
match the extent and resolution of a template \code{SpatRaster}. Requires an
internet connection.
}
\examples{
\dontrun{
dtm <- rast(system.file("extdata/dtms/dtmf.tif", package = "mesoclim"))
dem <- get_dem(dtm)
}
}
\keyword{preprocess}
